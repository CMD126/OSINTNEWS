# Core search engine
# Install ONE — code tries ddgs first, falls back to duckduckgo-search
duckduckgo-search>=6.0.0
# ddgs>=0.1.0              # alternative: uncomment if you prefer the ddgs package

# Terminal & UI helpers
colorama>=0.4.6

# Windows notifications (optional — fails silently if not installed)
plyer>=2.1.0

# Excel export
openpyxl>=3.1.0

# AI / RAG providers — install only what you use:
anthropic>=0.49.0          # Claude API (recommended) — claude-sonnet-4-6 etc.
openai>=1.50.0             # OpenAI GPT alternative
google-generativeai>=0.8.0 # Google Gemini (NEW) — gemini-1.5-flash / gemini-1.5-pro
requests>=2.32.0           # Ollama local API (free, offline)
