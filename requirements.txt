# ─────────────────────────────────────────────────────────────────────────────
#  OSINTNEWS — dependencies
#  Install everything:      pip install -r requirements.txt
#  Minimum to run:          pip install ddgs colorama
#  The auto-installer (modules/installer.py) also installs the Core block on
#  first launch, so a manual pip is only needed for offline / locked-down setups.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core (required) ──────────────────────────────────────────────────────────
ddgs>=6.0.0            # search engine — modern package (was duckduckgo-search)
colorama>=0.4.6        # terminal colours + Windows ANSI fix

# ── Recommended ──────────────────────────────────────────────────────────────
requests>=2.32.0       # Ollama local API + generic HTTP
openpyxl>=3.1.0        # Excel (.xlsx) export in the GUI

# ── Optional: desktop notifications ──────────────────────────────────────────
plyer>=2.1.0           # Windows toast notifications (fails silently if absent)

# ── Optional: AI / RAG providers — install only the one(s) you use ───────────
anthropic>=0.49.0             # Claude   — ANTHROPIC_API_KEY   (recommended)
openai>=1.50.0                # OpenAI   — OPENAI_API_KEY
google-generativeai>=0.8.0    # Gemini   — GOOGLE_API_KEY
# Ollama needs no package — just a running local server (see --provider ollama)

# ── Dev only: run the test suite with `pytest` ──────────────────────────────
pytest>=7.0.0
